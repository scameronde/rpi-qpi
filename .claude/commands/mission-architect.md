# Mission Architect

Use the Agent tool to invoke the `mission-architect` subagent.

Pass the user's initial project idea or feature description as the task. If the user provided no description, pass: "The user wants to start a new project. Begin the discovery conversation."

The agent will conduct a collaborative discovery conversation with the user and produce a mission statement document in `thoughts/shared/missions/`.
