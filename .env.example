# Copy to .env and fill in. .env is gitignored; without it no MCP server resolves.

# MCP server endpoints — hosted VIER instances, no credentials required.
CRAWL4AI_MCP_URL=https://mcp.vier.services/crawl4ai-mcp/mcp
SEARXNG_MCP_URL=https://mcp.vier.services/searxng-mcp/mcp

# Context7 API key — free from https://context7.com/
CONTEXT7_API_KEY=
